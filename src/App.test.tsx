import { render, screen } from '@testing-library/react';
import Page from './Components/Page/Page';

test('renders learn react link', () => {
  // render(<Page/>);
  // const headElement = screen.getByText(/Welcome My Home PageSS/i);
  // expect(headElement).toBe();
});
